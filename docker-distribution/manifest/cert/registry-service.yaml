apiVersion: v1
kind: Service
metadata:
  name: docker-registry
  namespace: cicd-instances
spec:
  ports:
  - port: 5000
    protocol: TCP
    targetPort: 5000
  selector:
    app: docker-registry
  sessionAffinity: None
  type: ClusterIP
